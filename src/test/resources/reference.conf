scenarios {
  targetPeriod = "201809"
}

api {
  sbr {
    control {
      protocol = "http"
      protocol = ${?ONS_SBR_CONTROL_API_PROTOCOL}
      host = "localhost"
      host = ${?ONS_SBR_CONTROL_API_HOST}
      port = 9000
      port = ${?ONS_SBR_CONTROL_API_PORT}
    }
  }
}
